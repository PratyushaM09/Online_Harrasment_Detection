# Milestone 2B keeps dependencies focused on configuration, testing, CSV loading, and splitting.
# Runtime ML/UI dependencies will be added incrementally when implemented.

iterative-stratification
pandas
PyYAML
pytest
